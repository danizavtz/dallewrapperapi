#!/usr/bin/env node
const app = require('../app');

try {
    app.listen(process.env.PORT);
    console.log('Microsservice openai image provider listening at http://localhost:%s', process.env.PORT);
} catch (e) {
    throw e;
}